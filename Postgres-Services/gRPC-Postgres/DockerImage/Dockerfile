# Используем минимальный образ, например, для Go приложения
FROM alpine:latest

# Копируем бинарный файл в контейнер
COPY ./postgres-grpc /app/


# порт gRPС
EXPOSE 50051

# Указываем команду, которая будет выполнена при старте контейнера
CMD ["/app/postgres-grpc"]

